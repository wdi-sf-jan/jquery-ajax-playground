{
  "name": "jquery-ajax-playground",
  "version": "1.0.0",
  "description": "A WDI exercise for taking a look at $.ajax",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "beefy src/index.js:dist/bundle.js --live -d",
    "bundle": "browserify src/index.js -o dist/bundle.js"
  },
  "author": "@zealoushacker",
  "license": "ISC",
  "dependencies": {
    "jquery": "^2.1.3"
  },
  "devDependencies": {
    "beefy": "^2.1.2",
    "browserify": "^8.1.1"
  }
}
